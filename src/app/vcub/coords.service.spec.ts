/*import {describe, it, expect, inject, beforeEach, beforeEachProviders} from '@angular/core/testing';
import {CoordsService} from './coords.service';

describe('Service: CoordsService', () => {
  let service;
  
  beforeEachProviders(() => [
    CoordsService
  ]);
  
  beforeEach(inject([CoordsService], s => {
    service = s; 
  }));
   
  it('should convert coords', () => {
    let latCar = 44.807260;
    let lngCar = -0.605569; 
    expect(service.meters2degrees(lngCar, latCar)).toBe(false);
  });
}) */
describe('1st tests', () => {
  it('true is true', () => expect(true).toBe(true));
}); 